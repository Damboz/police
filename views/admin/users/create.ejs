<%- include('../../partials/header') %>

<div class="app-wrapper">
    <%- include('../../partials/sidebar') %>

    <div class="main-content">
        <%- include('../../partials/topbar') %>

        <div class="container-fluid py-4">
            <div class="row justify-content-center">
                <div class="col-12 col-lg-8">
                    
                    <div class="card shadow-sm border-0">
                        <div class="card-header bg-navy text-white d-flex align-items-center justify-content-between py-3">
                            <h6 class="mb-0 fw-bold">
                                <i class="bi bi-person-plus-fill me-2 text-warning"></i>Register Station Personnel
                            </h6>
                            <a href="/admin/dashboard" class="btn btn-sm btn-outline-light">Back to Dashboard</a>
                        </div>

                        <div class="card-body p-4">
                            <% if (error) { %>
                                <div class="alert alert-danger alert-dismissible fade show small mb-3">
                                    <i class="bi bi-exclamation-triangle-fill me-2"></i><%= error %>
                                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                                </div>
                            <% } %>

                            <form action="/admin/users/create" method="POST">
                                <div class="row g-3">
                                    
                                    <!-- Badge Number -->
                                    <div class="col-md-6">
                                        <label for="badge_number" class="form-label">Badge / Service # *</label>
                                        <input type="text" class="form-control" id="badge_number" name="badge_number" value="<%= formData.badge_number || '' %>" placeholder="e.g. MPS-4821" required>
                                    </div>

                                    <!-- Rank Title -->
                                    <div class="col-md-6">
                                        <label for="rank_title" class="form-label">Official Rank *</label>
                                        <select class="form-select" id="rank_title" name="rank_title" required>
                                            <option value="">Select Rank...</option>
                                            <option value="Constable" <%= formData.rank_title === 'Constable' ? 'selected' : '' %>>Constable</option>
                                            <option value="Sergeant" <%= formData.rank_title === 'Sergeant' ? 'selected' : '' %>>Sergeant</option>
                                            <option value="Inspector" <%= formData.rank_title === 'Inspector' ? 'selected' : '' %>>Inspector</option>
                                            <option value="Assistant Superintendent" <%= formData.rank_title === 'Assistant Superintendent' ? 'selected' : '' %>>Assistant Superintendent</option>
                                            <option value="Superintendent" <%= formData.rank_title === 'Superintendent' ? 'selected' : '' %>>Superintendent</option>
                                            <option value="Station Commander" <%= formData.rank_title === 'Station Commander' ? 'selected' : '' %>>Station Commander</option>
                                        </select>
                                    </div>

                                    <!-- First Name -->
                                    <div class="col-md-6">
                                        <label for="first_name" class="form-label">First Name *</label>
                                        <input type="text" class="form-control" id="first_name" name="first_name" value="<%= formData.first_name || '' %>" required>
                                    </div>

                                    <!-- Last Name -->
                                    <div class="col-md-6">
                                        <label for="last_name" class="form-label">Last Name *</label>
                                        <input type="text" class="form-control" id="last_name" name="last_name" value="<%= formData.last_name || '' %>" required>
                                    </div>

                                    <!-- Email -->
                                    <div class="col-md-6">
                                        <label for="email" class="form-label">Official Email *</label>
                                        <input type="email" class="form-control" id="email" name="email" value="<%= formData.email || '' %>" required>
                                    </div>

                                    <!-- Phone Number -->
                                    <div class="col-md-6">
                                        <label for="phone_number" class="form-label">Phone Number</label>
                                        <input type="text" class="form-control" id="phone_number" name="phone_number" value="<%= formData.phone_number || '' %>" placeholder="+265 ...">
                                    </div>

                                    <!-- System Role -->
                                    <div class="col-md-6">
                                        <label for="role" class="form-label">System Access Role (RBAC) *</label>
                                        <select class="form-select" id="role" name="role" required>
                                            <option value="">Select Access Role...</option>
                                            <option value="Police Officer" <%= formData.role === 'Police Officer' ? 'selected' : '' %>>Police Officer / Desk Officer</option>
                                            <option value="Investigator" <%= formData.role === 'Investigator' ? 'selected' : '' %>>Investigator / Detective</option>
                                            <option value="Supervisor" <%= formData.role === 'Supervisor' ? 'selected' : '' %>>Supervisor / Station Commander</option>
                                            <option value="Admin" <%= formData.role === 'Admin' ? 'selected' : '' %>>System Administrator</option>
                                        </select>
                                    </div>

                                    <!-- Initial Password -->
                                    <div class="col-md-6">
                                        <label for="password" class="form-label">Initial Password *</label>
                                        <input type="password" class="form-control" id="password" name="password" minlength="6" required>
                                    </div>

                                </div>

                                <hr class="my-4 text-muted">

                                <div class="d-flex align-items-center justify-content-between">
                                    <a href="/admin/dashboard" class="btn btn-outline-secondary">Cancel</a>
                                    <button type="submit" class="btn btn-navy px-4 fw-bold">
                                        <i class="bi bi-check-circle me-1"></i> Complete Registration
                                    </button>
                                </div>
                            </form>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>
</div>

<%- include('../../partials/footer') %>